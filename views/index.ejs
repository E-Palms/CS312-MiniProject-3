<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<div class="bg-light" style="min-height: 100vh;">
    <nav class="navbar navbar-expand-lg navbar-light bg-secondary">
        <a class="navbar-brand" href="">The Blog</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link active" href="">Home <span class="sr-only">(current)</span></a>
                <a class="nav-item nav-link" href="">About</a>
                <a class="nav-item nav-link" href="">Account</a>
                <a class="nav-item nav-link" href="">Contact</a>
            </div>
        </div>
    </nav>

    <div class="container mw-100 row d-flex justify-content-center p-2">
        <form action="/submit" method="POST" id="postForm" class="row mw-75 bg-secondary p-2" style="border-radius: 35px;">
            <div class="col-sm-4 form-group d-flex justify-content-center">
                <h3 class="align-self-center">User: <%= userName %></h3>
                <input class="form-control" type="hidden" name="userName" value="<%= userName %>">
            </div>

            <div class="col-sm-6 form-group">
                <label for="userTitle">Title</label>
                <input class="form-control" type="text" name="userTitle" required>
            </div>

            <div class="col-sm-2 form-group d-flex justify-content-center p-4">
                <input class="btn btn-light form-control" type="submit" value="submit">
            </div>

            <div class=" col-sm-12 form-group">
                <label for="userContent">Write your blog post here:</label>
                <textarea class="form-control" rows="8" name="userContent" required></textarea>
            </div>
        </form>
        <div class="col-sm-12 d-flex justify-content-center pt-4">
            <% if (error) { %>
                <div class="alert alert-danger w-25">
                  <%= error %>
                </div>
              <% } %>
        </div>
    </div>

    <% if (blogs && blogs.length > 0) { %>
        <% blogs.forEach((blogpost, blogidx) => { %>
            <div class="mw-100  d-flex justify-content-center p-2">
                <div class="w-50 bg-secondary row d-flex justify-content-center border p-2" style="border-radius: 35px;">
                    <div class="col-sm-12">
                        <h3 class="text-light"><%= blogpost.title %></h3>
                        <h4 class="text-light"><%= blogpost.content %></h4> 
                    </div>
                    <div class="col-sm-8 d-flex align-bottom flex-row pt-3">
                        <span class="align-bottom text-light"><%= "Posted by: " + blogpost.name + " on " + new Intl.DateTimeFormat('en', { month: 'short' }).format(blogpost.date_created) + "-" + new Intl.DateTimeFormat('en', { day: '2-digit' }).format(blogpost.date_created) + "-" +  new Intl.DateTimeFormat('en', { year: 'numeric' }).format(blogpost.date_created); %></span>
                    </div>
                    <div class="col-sm-4 d-flex align-middle flex-row-reverse p-1">
                        <form action="/delete" method="GET"  class="p-1">
                            <input type="hidden" name="postId" value="<%= blogpost.blog_id %>">
                            <button class="btn" type="submit" value="delete">Delete</button>
                        </form>
                        <form action="/edit" method="GET"  class="p-1">
                            <input type="hidden" name="postId" value="<%= blogpost.blog_id %>">
                            <button class="btn" type="submit" value="edit"> Edit </button>
                        </form>
                    </div>
                </div>
            </div>
        <% }); %>
    <% } %>
    </div>
    <script src="https://code.jquery.com/jquery-2.1.3.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</div>
</html>